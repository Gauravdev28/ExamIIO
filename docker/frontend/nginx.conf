server {
    listen 80;
    server_name localhost;

    # Static assets and SPA frontend routing
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Proxy REST API requests to Django backend
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Proxy WebSocket requests to Django Channels
    location /ws/ {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }

    # Deny direct public access to private media.
    # All sensitive media (proctoring evidence, student recordings, reports, certificates, DSAR exports)
    # MUST be downloaded only through authenticated Django endpoints with RBAC permission enforcement.
    location ^~ /media/ {
        deny all;
        return 403;
    }

    # Django admin staticfiles
    location /static/ {
        alias /app/staticfiles/;
    }
}
